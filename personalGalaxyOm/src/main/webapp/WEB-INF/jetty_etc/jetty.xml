<?xml version="1.0"?>
<!DOCTYPE Configure PUBLIC "-//Jetty//Configure//EN" "File:///F:/card-om/bxbank/cmc/om/src/main/webapp/WEB-INF/jetty_etc/configure_9_3.dtd">
<Configure class="org.eclipse.jetty.server.Server" id="Server">
	<!-- =========================================================== -->
	<!-- Configure the Server Thread Pool.                           -->
	<!--                                                             -->
	<!-- Consult the javadoc of o.e.j.util.thread.QueuedThreadPool   -->
	<!-- for all configuration that may be set here.                 -->
	<!-- =========================================================== -->
	<Get name="ThreadPool">
		<Set name="minThreads" type="int">10</Set>
		<Set name="maxThreads" type="int">200</Set>
		<Set name="idleTimeout" type="int">60000</Set>
		<Set name="detailedDump">false</Set>
	</Get>

	<Call name="addConnector">
		<Arg>
			<New class="org.eclipse.jetty.server.ServerConnector">
				<Arg name="server"><Ref refid="Server"/></Arg>
				<Set name="host"><Property name="jetty.host"/></Set>
				<Set name="port"><Property default="18889" name="jetty.http.port"/></Set>

			</New>
		</Arg>
	</Call>
	<Set name="handler">
		<New class="org.eclipse.jetty.server.handler.HandlerList" id="Handlers">
			<Set name="handlers">
				<Array type="org.eclipse.jetty.server.Handler">
					<Item>
						<New class="org.eclipse.jetty.server.handler.ContextHandlerCollection" id="Contexts"/>
					</Item>
					<Item>
						<New class="org.eclipse.jetty.server.handler.DefaultHandler" id="DefaultHandler"/>
					</Item>
					<Item>
						<New class="org.eclipse.jetty.server.handler.StatisticsHandler" id="StatsHandler"/>
					</Item>
				</Array>
			</Set>
		</New>
	</Set>

</Configure>