
$(document).ready(function() {
	getPkList({
		url: contextPath + "/baseCommon/pklistBase?tableName=FM_COMPANY&tableCol=COMPANY,COMPANY_NAME",
		id: "company",
		async: false
	});

	$("#cifContactTypeAdd").Validform({
		tiptype:2,
		callback:function(form){
			cifContactTypeAdd();
			return false;
		}
	});

	$(".select2").select2();
});

function cifContactTypeAdd(){
	var paraJson,generalFieldsJson,keyFieldsJson;
	paraJson = {};
	generalFieldsJson={};
	keyFieldsJson={};
	var url = contextPath+"/baseCommon/updateAndInsertForSave";
	paraJson.tableName="CIF_CONTACT_TYPE";
		keyFieldsJson.CONTACT_TYPE=$("#contactType").val();
		generalFieldsJson.CONTACT_TYPE_DESC=$("#contactTypeDesc").val();
		generalFieldsJson.ROUTE=$("#route").val();
		generalFieldsJson.SWIFT_ID=$("#swiftId").val();
		generalFieldsJson.COMPANY=$("#company").val();
	paraJson.key = keyFieldsJson;
	paraJson.general=generalFieldsJson;
	paraJson.reqNum = parent.parent.$(".breadcrumb").data("reqNum");
	var params = {
		paraJson:JSON.stringify(paraJson)
	};
	sendPostRequest(url,params, callback_cifContactTypeAdd,"json");
}

function callback_cifContactTypeAdd(json){
	if (json.success) {
     parent.showMsgDuringTime("添加成功");
	} else if (json.errorMsg) {
		showMsg(json.errorMsg, 'errorMsg');
	}
}

function cifContactTypeAddCancel(){
	var index = parent.layer.getFrameIndex(window.name);  //获取窗口索引
    parent.layer.close(index);  //关闭窗口
}


