package com.dcits.orion.stria.test.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.alibaba.fastjson.JSONObject;
import com.dcits.galaxy.base.data.BeanResult;

public class TestService {
	private static final Logger log = LoggerFactory
			.getLogger(TestService.class);

	public void test1(JSONObject in) {
		log.debug("测试方法1，活期存入提交完成！");
	}

	public BeanResult test2(String baseAcctNo, String ccy) {
		log.debug("测试方法2，baseAcctNo[" + baseAcctNo + "] ccy[" + ccy + "]");
		return new BeanResult();
	}

	public BeanResult test3(double tranAmt) {
		log.debug("测试方法3，tranAmt[" + tranAmt + "]");
		return new BeanResult();
	}

	public BeanResult test4(double tranAmt) {
		log.debug("测试方法4，tranAmt[" + tranAmt + "]");
		return new BeanResult();
	}
}
